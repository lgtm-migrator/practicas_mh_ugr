- BIN: Únicamente contiene los datasets y la tabla excel rellena. Esta última
también esta adjunta en la memoria.

- FUENTES: Este directorio contiene tanto el ejecutable como el resto de códigos fuente.
El lenguaje de programación usado es Python3 y por comodidad el programa principal
también se ha almacenado en ese directorio.

      - practica1.py : Programa a ejecutar (main)

      - algoritmos/* : Los diferentes algoritmos implementados. Un fichero por algoritmo.

      - output/*     : Aquí se almacena las salidas de las diferentes ejecuciones.

      - requirements.txt : Fichero con todas las dependencias a instalar. No se tiene
      que utilizar, a no ser que haya algún error con alguna dependencia.

      - install.sh   : Script instalador de dependencias. Crea un entorno virtual
      y descarga todas las dependencias. Este es el único fichero que hay que ejecutar
      antes de poder utilizar la aplicación. Nota: No recibe argumentos.

      NOTA: Si existe algun fallo al instalar el paquete pykdtree es debido a que su
      sistema no soporta OpenMP o al menos el compilador por defecto. Este error se puede
      obviar ya que la aplicación utilizará otra implementación si no se encuentra dicho
      paquete.

- practica1.pdf: La memoria.
